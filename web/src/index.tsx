import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// aqui enviamos tudo para o html;

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

